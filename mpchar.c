#include "monty.h"
/**
  *d_nop- nothing
  *@head: stack head
  *@count: line_number
  *Return: nothing
 */
void d_nop(stack_t **head, unsigned int count)
{
	(void) count;
	(void) head;
}
L%d: can't pchar, value out of range\n", count);
		fclose(da.file);
		free(da.content);
		free_stack(*head);
		exit(EXIT_FAILURE);
	}
	printf("%c\n", h->n);
}
