#include "main.h"
/**
 *_abs - compute the absolute value of integer
 * @c: the number to be computed
 * Return: c
 */

int _abs(int c)
{
	if (c < 0)
	{
		return (-c);
	}
	else
	{
		return (c);
	}
	return (c);
}
